---

spring:
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  output:
    ansi:
      enabled: always

# BEGIN
rsa:
  private-key: classpath:certs/private.pem  # Должен быть задан в переменных окружения или напрямую
  public-key: classpath:certs/public.pem
# END